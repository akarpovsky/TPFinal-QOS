Para hacer la migración:

1) Ejecutar _create.sql

2) Ejecutar las primeras líneas del update.sql (hasta el comentario que dice "ejecutar DUMP")

3) Ejecutar spring2_dump.sql

4) Ejecutar todas las líneas que restaban ejecutar del archivo update.sql